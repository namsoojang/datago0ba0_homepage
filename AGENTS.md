# 에이전트 공동 작업 규칙 (SOP)

이 문서는 이 저장소에서 작업하는 모든 AI 에이전트가 따르는 **유일한 규칙 원본**입니다.
`CLAUDE.md`, `GEMINI.md`는 이 문서를 가리키는 포인터일 뿐이므로, 규칙을 고칠 때는 항상 이 파일만 수정합니다.

대상 에이전트: **Claude (Claude Code)**, **Gemini (Antigravity)**, **Codex (Cursor / CLI)**

---

## 1. 기본 원칙 — 한 번에 한 에이전트

* 역할(구현/리뷰/기획)은 **고정하지 않습니다.** 사용자가 안건마다 담당을 지정합니다.
* **실제 파일을 수정하는 에이전트는 언제나 1개입니다.** 나머지는 읽기 전용으로만 참여합니다.
* 모든 작업은 메인 폴더 한 곳에서 진행합니다.
  `C:\Users\장남수\Documents\00_데이터공방\0_홈페이지`
* 별도 worktree나 병렬 브랜치 작업은 사용하지 않습니다. 필요하면 사용자가 먼저 지시합니다.

### 1-1. 이미지 제작은 Codex가 담당합니다 (역할 고정의 유일한 예외)

역할을 고정하지 않는다는 원칙의 예외입니다. **이미지를 새로 만드는 작업은 안건과 무관하게 항상 Codex가 맡습니다.**
블로그 썸네일, 본문 삽화, 카드뉴스, 배너가 모두 해당합니다. 2026-07-22 같은 썸네일을 여러 에이전트가 만들어 비교한 결과 사용자가 지정했습니다.

* Claude와 Gemini는 이미지를 직접 생성하지 않습니다. 필요하면 사용자에게 Codex 요청을 안내합니다.
* 문구 변경 등으로 재제작이 필요할 때도 Codex에게 요청합니다.
* 받은 이미지를 저장소에 배치하고 경로·규격·용량·렌더링을 확인하는 일은 그 안건의 파일 담당 에이전트가 합니다.
* 블로그 썸네일 규격은 **1200×630(1.9:1)** 입니다. 카드가 418×220으로 표시되고 `object-fit: cover`라 비율이 어긋나면 가장자리가 잘립니다. 색상은 `DESIGN.md` 팔레트를 따르고, 파일은 `assets/images/blog/{주제}-{YYYYMMDD}.png` 로 둡니다.

## 2. 작업 시작 전 확인

작업을 시작하기 전에 반드시 아래를 확인하고, 예상과 다르면 **손대지 말고 사용자에게 보고**합니다.

```bash
git status
git branch --show-current
git log --oneline -5
```

* 미커밋 변경이 남아 있으면 다른 에이전트나 사용자의 작업일 수 있습니다. 내가 만든 것이 아니면 건드리지 않습니다.
* 미커밋 변경이 있는 상태에서는 **브랜치를 전환하지 않습니다.**
* **예상보다 훨씬 많은 파일이 변경되어 보이면 즉시 작업을 멈추고 사용자에게 보고합니다.** 특히 줄바꿈만 바뀐 전면 변경으로 보인다면 아래 2-1을 먼저 확인하십시오.

### 2-1. Git 실행 환경은 Windows Git으로 통일합니다

이 저장소는 `core.autocrlf=true` 환경에서 CRLF로 저장되어 있습니다.
**WSL·Linux·Git Bash 등 `autocrlf`가 꺼진 Git으로 같은 폴더의 상태를 확인하면 전 파일이 "수정됨"으로 잘못 표시됩니다.** 실제로 2026-07-22에 이 오탐으로 "59개 파일·12,000줄 변경"이 보고된 사례가 있습니다.

* `git status`, `git diff`, `git worktree` 등 상태 확인은 **Windows Git(PowerShell / CMD)** 으로 실행합니다.
* 다른 환경에서 대량 변경이 보이면 **되돌리거나 정리하지 말고**, Windows Git으로 재확인한 뒤 보고합니다.

## 3. 금지 명령

사용자가 명시적으로 요청하지 않는 한 아래 명령은 사용하지 않습니다. 남의 작업을 되돌릴 수 없게 지웁니다.

```text
git add .        git add -A        git commit -am
git restore      git checkout -- <file>
git stash        git clean         git reset --hard
```

`git add -u`는 저장소 전체에 사용하지 않습니다. 변경 파일이 많을 때는 **자신이 담당한 경로로 범위를 한정해서만** 허용합니다.

```bash
git add -u -- modules/          # 담당 범위가 명확할 때만
git diff --cached --stat
```

## 4. 커밋

* 자신이 수정한 파일만 **경로를 지정해** 스테이징하고, `git diff --cached`로 내용을 확인한 뒤 커밋합니다.
* **커밋 전에 동작을 확인합니다.** 페이지를 수정했으면 브라우저에서 렌더링을 확인하고, 확인하지 못했으면 "미검증"이라고 사용자에게 명시적으로 알립니다. "수정 완료"로만 끝내지 않습니다.
* 커밋 메시지에 아래 트레일러를 붙입니다. 트레일러 앞에는 빈 줄이 있어야 합니다.
* **`Agent`만 필수**입니다. `Model`은 알면 적고, `Reviewed-by`는 실제로 다른 에이전트의 리뷰를 거쳤을 때만 적습니다.
* 작업 시각은 Git이 자동 기록하므로 따로 적지 않습니다.

```text
feat: 문의 폼 전환 흐름 개선

무엇을 왜 바꿨는지 1~3줄.

Agent: Claude
Model: claude-opus-4-8
Reviewed-by: Codex
```

필수 항목만 쓰면 이것으로 충분합니다.

```text
fix: 모바일 메뉴 겹침 수정

Agent: Gemini
```

담당자별 조회:

```bash
git log --format='%h %s %(trailers:key=Agent,valueonly)'
```

## 5. 에이전트 간 의견 교환

코드를 고치기 전에 여러 에이전트의 의견이 필요하면, **파일을 수정하지 않고** 문서로 주고받습니다.

* 위치: `.tmp/{안건명}/` (Git에 올라가지 않음)
* 파일명: `{YYYYMMDD_HHMMSS}_{작성자}.md` — **작성자 기준**입니다. 수신자는 파일명이 아니라 프런트매터에 적습니다.
* 기존 문서는 덮어쓰지 않고 항상 새 파일을 만듭니다.
* 다른 에이전트에게 전달할 때는 **절대경로**를 알려줍니다.
* 프런트매터 필수 항목:

```yaml
created_at: 2026-07-22T15:05:20+09:00
author_name: Claude
model: claude-opus-4-8
recipient: Codex
topic: 안건 한 줄 요약
status: draft | review-requested | reviewed
```

코드 변경을 동반하는 논의라면 `related_branch`, `related_commit`을 추가합니다.

안건에 담당이 정해지면 `.tmp/{안건명}/_owner.md`에 기록합니다.
역할을 고정하지 않으므로 "구현/리뷰"가 아니라 **현재 작성자와 다음 담당**을 적습니다.

```text
| 안건 | 현재 작성자 | 작업 범위 | 상태 | 시작 시각 | 다음 담당 |
|---|---|---|---|---|---|
| PPT 템플릿 다크 테마 | Gemini | ppt-template.html | 구현중 | 2026-07-22 15:00 | Claude |
```

`_owner.md`는 **덮어쓰기 금지 규칙의 유일한 예외**입니다. 현재 상태를 나타내는 상태판이므로 계속 갱신합니다.
의견 문서는 반드시 새 파일로 만듭니다.

## 6. 사용자 승인이 필요한 작업

아래는 계획을 먼저 공유하고 승인을 받은 뒤에 진행합니다.

* 추적 중인 파일의 이동·삭제·이름 변경, 폴더 구조 변경
* 신규 라이브러리·스크립트 등 의존성 추가
* 배포, 원격 푸시
* 브랜치 병합

브랜치 생성·전환은 다음 조건을 모두 만족할 때만 별도 승인 없이 진행합니다.
그 외에는 승인을 받습니다.

* 사용자가 "이 작업은 별도 브랜치에서 진행"이라고 지정했고
* 작업 트리가 깨끗하며
* 사용자가 지정한 작업 범위 안일 때

작업이 끝나면 `git status`를 다시 확인하고, 남은 변경과 임시 문서 경로를 사용자에게 보고합니다.

## 7. 파일 정리

* 서버에서 직접 참조하지 않는다는 이유만으로 파일을 옮기거나 지우지 않습니다. 테스트·설정·문서는 그대로 둡니다.
* `.tmp`에는 **일회성 토론 초안과 검토 자료만** 넣습니다.
* `.tmp` 문서는 Git에 남지 않습니다. 사용자가 결론을 확정하면 **대화 원문을 옮기지 말고, 결론만 요약해** `AGENTS.md` 또는 `docs/` 아래 새 문서로 작성합니다. 날짜·변경 내용·사유를 남깁니다. 원문을 그대로 옮기면 불필요한 토론 과정까지 저장소에 들어갑니다.
* `.claude/`, `.idea/` 등 도구별 로컬 설정은 Git에 올리지 않습니다. 사용자별 절대경로와 권한 설정이 들어 있어 공유 가치가 없습니다.

## 8. 참고 문서

* 디자인·설계 기준: `DESIGN.md`
* 프로젝트 구조: `file_tree.md`
