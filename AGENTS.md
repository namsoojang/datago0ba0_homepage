# Agent Working Rules (SOP)

이 문서는 **Gemini (Antigravity)**와 **Codex (Cursor Plus)** 두 에이전트가 동일한 프로젝트에서 협업할 때 발생할 수 있는 충돌을 방지하고 상호 보완적으로 작동하도록 정의된 SOP(표준 운영 절차)입니다.

---

## 1. 역할 정의 (Roles)

### 🟢 Gemini / Antigravity (메인 개발 에이전트)
* **주요 역할**: 핵심 기능 구현, 신규 페이지 추가, 전반적인 비즈니스 로직 작성.
* **작업 공간**: `0_홈페이지` (메인 폴더 / `main` 브랜치)
* **작업 원칙**: 
  - 대대적인 신규 개발을 마친 후 구현 요약(구현 방식, 구조 변경사항 등)을 Git 커밋 메시지 또는 안내 텍스트로 자세히 남깁니다.
  - Codex의 검토와 보완이 필요한 부분에 대해 주석이나 가이드를 남겨 협업을 유도합니다.

### 🔵 Codex CLI (검토, 테스트 및 디버깅 에이전트)
* **주요 역할**: 코드 리뷰, 에러 디버깅, 테스트 코드 작성, 소규모 리팩토링 및 성능 최적화.
* **작업 공간**: `0_홈페이지_codex` (Worktree 폴더 / `review/codex` 브랜치)
* **작업 원칙**:
  - Gemini가 구현한 코드를 무작위로 처음부터 다시 짜지(Rebuild) 않습니다. Gemini의 설계 의도를 최대한 존중하고 보존합니다.
  - 가급적 작고 안전한 단위로 리팩토링 및 디버깅을 수행합니다.

---

## 2. 작업 공간 및 Git 정책 (Folder & Git Policy)

### 📂 폴더 및 충돌 방지 정책
* **동시 편집 금지**: 두 에이전트가 하나의 물리 폴더를 동시에 수정하지 않습니다.
* **영역 격리**:
  - Gemini는 항상 `0_홈페이지`에서 작업합니다.
  - Codex는 항상 `0_홈페이지_codex`에서 작업합니다.
* **상호 조율**: 다른 에이전트가 편집 중이거나 작업 예정인 파일에 대해 충돌 가능성이 있는 경우, 즉시 작업을 멈추고 사용자에게 상황을 알립니다.

### 🌿 Git 워크플로우 (Sync Loop)
`git worktree`로 물리적으로 격리된 상태에서 아래 순서에 맞춰 코드를 동기화합니다.

1. **Gemini가 개발 완료**: `0_홈페이지`에서 작업을 마친 후 스테이징 및 로컬 커밋을 수행합니다.
   ```bash
   git status
   git add .
   git commit -m "feat: 홈페이지 소개 섹션 레이아웃 완성"
   ```
2. **Codex가 내용 반영**: `0_홈페이지_codex` 터미널에서 Gemini가 작업한 브랜치(`main`)의 내용을 병합(Merge)합니다.
   ```bash
   git checkout review/codex
   git merge main
   ```
3. **Codex 검토 및 수정**: Codex가 수정이 필요한 사항(버그 픽스, 테스트 코드 추가 등)을 수정한 뒤 커밋합니다.
   ```bash
   git add .
   git commit -m "fix: 소개 섹션 모바일 반응형 줄바꿈 오류 수정 및 테스트"
   ```
4. **최종 병합 (Main)**: Gemini 폴더(`0_홈페이지`)로 돌아와 Codex가 기여한 브랜치를 최종 병합합니다.
   ```bash
   git merge review/codex
   ```

---

## 3. 공통 규칙 (Global Rules)
* **의존성(Dependency) 추가 제한**: 명확한 이유나 사용자 승인 없이 신규 npm 라이브러리나 스크립트를 임의로 추가하지 않습니다.
* **구조적 변경 사전 승인**: 파일명을 변경하거나, 폴더 구조를 통째로 옮기거나, 파일을 삭제하는 등 프로젝트 구조에 큰 변화를 줄 때는 무조건 작업 전에 계획을 공유하고 사용자 승인을 받습니다.
